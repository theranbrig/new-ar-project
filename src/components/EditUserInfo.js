import React, { useState, useEffect } from 'react';
import styled from 'styled-components';

export const EditUserStyles = styled.div``;

const EditUserInfo = ({description, photo}) => {
  return (
    <EditUserStyles>
      <textarea />
    </EditUserStyles>
  );
};

export default EditUserInfo;
